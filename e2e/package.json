{
  "name": "fogofwalk-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:all": "bun run test && bun run test:performance",
    "test:trails:production": "playwright test --config=trails-production.config.ts specs/trails.spec.ts",
    "test:performance": "playwright test --config=performance.config.ts",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install chromium",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
